// // pages/app/(course)/courses/[courseId]/quizzes/[quizId]/quizpage.tsx
// import StudentQuizAttempt from './studentquizattempt';

// const QuizPage = ({ params }: { params: { courseId: string, quizId: string } }) => {
//   return <StudentQuizAttempt courseId={params.courseId} quizId={params.quizId} />;
// };

// export default QuizPage;
